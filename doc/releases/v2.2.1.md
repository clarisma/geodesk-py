## Performance enhancements

- Spatial predicates involving large relations (e.g. country boundaries) are now up to 30x faster

## Bug fixes

- `contains()` now prevent false positives if any of the feature's tiles are missing (clarisma/libgeodesk#43)

- `Features.length()` now properly returns 0 for nodes




